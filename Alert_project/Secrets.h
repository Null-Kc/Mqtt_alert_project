const char *ssid = ""; // Cria a variavel ssid onde sera armarzenado o nome do WIFI que o ESP se conectara
const char *password = "";  // Cria a variavel password onde sera armarzenado a sebga do WIFI que o ESP se conectara

const char *mqtt_username = ""; // Cria a variavel mqtt_username onde fica armazenado o login que sera usado para conectar no Broker
const char *mqtt_password = ""; // Cria a variavel mqtt_password onde fica armazenado a senha que sera usado para conectar no Broker
